// Variáveis
double media = 8;
int faltas = 10;

// Operador E
Console.WriteLine(media >= 7 && faltas < 10);

// Operador OU
Console.WriteLine(media >= 7 || faltas < 10);

// Operador de NEGAÇÃO
Console.WriteLine(!true);

// Operador XOR
Console.WriteLine(media >= 7 ^ faltas < 10);